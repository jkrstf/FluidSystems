<UserControl
    x:Class="FluidSystems.UI.WPF.Views.ControlPanels.ManifoldCleanerUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FluidSystems.UI.WPF.Views.ControlPanels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:Background="White"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" MinWidth="100" />
        </Grid.ColumnDefinitions>
        <TextBlock Margin="4" Text="Select sink" />
        <ComboBox
            Grid.Column="1"
            Margin="4"
            DisplayMemberPath="Name"
            ItemsSource="{Binding Sinks}"
            SelectedItem="{Binding SelectedSink}" />
        <Button
            Grid.Row="1"
            Grid.ColumnSpan="2"
            Height="25"
            Margin="4"
            Command="{Binding CleanManifoldCommand}"
            Content="Clean manifold" />
    </Grid>
</UserControl>