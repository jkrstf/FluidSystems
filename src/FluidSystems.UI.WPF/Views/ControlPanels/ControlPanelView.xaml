<UserControl
    x:Class="FluidSystems.UI.WPF.Views.ControlPanels.ControlPanelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FluidSystems.UI.WPF.Views.ControlPanels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vmCtrl="clr-namespace:FluidSystems.UI.WPF.ViewModels.ControlPanels"
    d:Background="White"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vmCtrl:FillChamberViewModel}">
            <local:FillChamberViews />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vmCtrl:EmptyingChamberViewModel}">
            <local:EmpyingChamberViews />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vmCtrl:ManifoldCleanerViewModel}">
            <local:ManifoldCleanerUserControl />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vmCtrl:ManualControlViewModel}">
            <local:ManualControlViews />
        </DataTemplate>
    </UserControl.Resources>
    <TabControl>
        <TabItem Header="Fill Chamber">
            <ContentControl Content="{Binding FillChamberViewModel}" />
        </TabItem>
        <TabItem Header="Empty Chamber">
            <ContentControl Content="{Binding EmptyChamberViewModel}" />
        </TabItem>
        <TabItem Header="Manifold  Cleaner">
            <ContentControl Content="{Binding ManifoldCleanerViewModel}" />
        </TabItem>
        <TabItem Header="Manual Control">
            <ContentControl Content="{Binding ManualControlViewModel}" />
        </TabItem>
    </TabControl>
</UserControl>
