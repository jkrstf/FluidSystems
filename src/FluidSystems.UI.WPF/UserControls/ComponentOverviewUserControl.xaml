<UserControl
    x:Class="FluidSystems.UI.WPF.UserControls.ComponentOverviewUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FluidSystems.UI.WPF.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock FontWeight="Bold" Text="Selected component:" />
            <TextBlock Text="{Binding SelectedComponentId}" />
            <Separator />
            <TextBlock FontWeight="Bold" Text="Material:" />
            <TextBlock Text="{Binding Material}" />
            <Separator />
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBlock FontWeight="Bold" Text="Component Parameters" />
            <ItemsControl ItemsSource="{Binding ComponentParameters}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Key}" />
                            <TextBlock Grid.Column="1" Text="{Binding Value}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Separator />
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock FontWeight="Bold" Text="Behavior Parameters" />
            <ItemsControl ItemsSource="{Binding BehaviorParameters}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Key}" />
                            <TextBlock Grid.Column="1" Text="{Binding Value}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Separator />
        </StackPanel>

        <StackPanel Grid.Row="3" VerticalAlignment="Bottom">
            <TextBlock Text="Actions" />
            <Button
                Command="{Binding ToggleComponentCommand}"
                CommandParameter="{Binding SelectedComponentId}"
                Content="Activate / Toggle" />
        </StackPanel>
    </Grid>
</UserControl>
